export const routePath = {
  home: '/',
  levelSelect: '/levels',
  magnetsGame: '/magnetsGame/:levelId', // Parameter for level ID
  electromagnetsGame: '/electromagnetsGame/:levelId', // Parameter for level ID
  sandbox: '/sandbox', // Sandbox route
  quiz: '/quiz', // New quiz route
};
